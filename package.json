{
  "name": "koburobu",
  "version": "0.1.0",
  "description": "This project is for handling blob storage in Azure",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && cp src/index.html dist/index.html && cp src/style.css dist/style.css && mkdir -p dist/assets && cp -r assets/* dist/assets/",
    "watch": "tsc -w",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron ."
  },
  "keywords": [],
  "author": "",
  "license": "LGPLv3+NoEvil",
  "devDependencies": {
    "@types/marked": "^5.0.2",
    "electron": "^40.0.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@azure/storage-blob": "^12.30.0",
    "lucide": "^0.563.0",
    "marked": "^17.0.1"
  }
}